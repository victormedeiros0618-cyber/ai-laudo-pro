/**
 * Re-export para compatibilidade com componentes shadcn/ui
 * que importam de "@/hooks/use-mobile".
 *
 * A implementação real está em useDeviceDetection.ts.
 */
export { useIsMobile } from './useDeviceDetection';
